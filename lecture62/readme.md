Hook: It is just a utility by which we can hook onto the features provided by the react and use them in our functional components. It is a special function which allows us to hook into the react features. 

